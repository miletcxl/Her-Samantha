# CLAUDE.md

Follow `AGENTS.md` for project rules and architecture.

Key reminders:
- Do NOT expand MVP scope
- Do NOT turn Her-Samantha into a Pi-only wrapper
- Do NOT expose private chain-of-thought
- Keep runtime adapters capability-based (`RuntimeCapabilities`)
- Do NOT hard-code Pi concepts into core
- CI tests use mocks only; real Pi/TTS are manual smoke tests
